---
description: Commit and push using conventional commit syntax
---

Commit and push: $ARGUMENTS

Use conventional commit syntax (e.g. `feat:`, `fix:`, `chore:`, `docs:`, `refactor:`, `test:`, `ci:`, `ignore:`). Stage any relevant changes, write a concise conventional commit message that captures the intent, then push to the remote.
