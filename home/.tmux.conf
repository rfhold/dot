set -g default-terminal "tmux-256color"

# Undercurl
set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'  # undercurl support
set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'  # underscore colours - needs tmux-3.0

# Aura colors for Tmux

set -g mode-style "fg=#bb9af7,bg=#4a4b6a"

set -g message-style "fg=#bb9af7,bg=#4a4b6a"
set -g message-command-style "fg=#bb9af7,bg=#4a4b6a"

set -g pane-border-style "fg=#4a4b6a"
set -g pane-active-border-style "fg=#e879f9"

set -g status "on"
set -g status-justify "left"

set -g status-style "fg=#bb9af7,bg=#1a1b26"

set -g status-left-length "100"
set -g status-right-length "100"

set -g status-left-style NONE
set -g status-right-style NONE

set -g status-left "#[fg=#1a1b26,bg=#bb9af7,bold] #S #[fg=#bb9af7,bg=#1a1b26,nobold,nounderscore,noitalics]"
set -g status-right "#[fg=#1a1b26,bg=#1a1b26,nobold,nounderscore,noitalics]#[fg=#bb9af7,bg=#1a1b26] #{prefix_highlight} #[fg=#4a4b6a,bg=#1a1b26,nobold,nounderscore,noitalics]#[fg=#bb9af7,bg=#4a4b6a] %Y-%m-%d  %I:%M %p #[fg=#bb9af7,bg=#4a4b6a,nobold,nounderscore,noitalics]#[fg=#1a1b26,bg=#bb9af7,bold] #h "
if-shell '[ "$(tmux show-option -gqv "clock-mode-style")" == "24" ]' {
  set -g status-right "#[fg=#1a1b26,bg=#1a1b26,nobold,nounderscore,noitalics]#[fg=#bb9af7,bg=#1a1b26] #{prefix_highlight} #[fg=#4a4b6a,bg=#1a1b26,nobold,nounderscore,noitalics]#[fg=#bb9af7,bg=#4a4b6a] %Y-%m-%d  %H:%M #[fg=#bb9af7,bg=#4a4b6a,nobold,nounderscore,noitalics]#[fg=#1a1b26,bg=#bb9af7,bold] #h "
}

setw -g window-status-activity-style "underscore,fg=#c0caf5,bg=#1a1b26"
setw -g window-status-separator ""
setw -g window-status-style "NONE,fg=#c0caf5,bg=#1a1b26"
setw -g window-status-format "#[fg=#1a1b26,bg=#1a1b26,nobold,nounderscore,noitalics]#[default] #I  #W #F #[fg=#1a1b26,bg=#1a1b26,nobold,nounderscore,noitalics]"
setw -g window-status-current-format "#[fg=#1a1b26,bg=#4a4b6a,nobold,nounderscore,noitalics]#[fg=#e879f9,bg=#4a4b6a,bold] #I  #W #F #[fg=#4a4b6a,bg=#1a1b26,nobold,nounderscore,noitalics]"

# tmux-plugins/tmux-prefix-highlight support
set -g @prefix_highlight_output_prefix "#[fg=#e0af68]#[bg=#1a1b26]#[fg=#1a1b26]#[bg=#e0af68]"
set -g @prefix_highlight_output_suffix ""

# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator

# decide whether we're in a Vim process
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"

bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h' 'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j' 'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k' 'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l' 'select-pane -R'

tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'

if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -n 'C-Space' if-shell "$is_vim" 'send-keys C-Space' 'select-pane -t:.+'

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l
bind-key -T copy-mode-vi 'C-Space' select-pane -t:.+

is_idf="ps -t '#{pane_tty}' | grep -iqE 'idf.py'"

bind-key -n 'C-c' if-shell "$is_idf" 'send-keys C-]' 'send-keys C-c'

# ============================================================================
# Tmux Plugin Manager (TPM)
# ============================================================================

# tmux-fzf configuration (must be set BEFORE loading plugins)
# Launch with: prefix + Ctrl-f
TMUX_FZF_LAUNCH_KEY="C-f"

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'sainnhe/tmux-fzf'

# Initialize TPM (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
